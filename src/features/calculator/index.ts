import type { TradeInput, TradeResult } from '@/types';

/**
 * Risk calculator. CLAUDE.md section 7.4.
 * Pure, offline, local-only — no network, no recommendations.
 *
 * SKELETON: not implemented. Wire up in a future "Day".
 */
export function calculateTrade(_input: TradeInput): TradeResult {
  throw new Error('calculateTrade not implemented yet');
}
